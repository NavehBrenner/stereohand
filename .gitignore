# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# venv / uv (library: don't pin a lockfile)
.venv/
venv/
uv.lock

# Local calibration / model artifacts (not checked in)
stereo_calib.json
*.task

# OS
.DS_Store

# qualety's generated engine configs + cache (regenerated every run).
# The committed config is qualety.config.json at the repo root.
.qualety/
